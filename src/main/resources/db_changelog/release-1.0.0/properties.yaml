databaseChangeLog:
  - property:
      name: default.schema
      value: ${spring.liquibase.default-schema}
      dbms: postgresql
  - property:
      name: app.username
      value: ${app.username}
      dbms: postgresql