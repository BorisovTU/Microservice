echo "SET HEADING OFF FEEDBACK OFF PAGESIZE 0
SELECT owner || '.' || object_name FROM dba_objects WHERE object_type = 'PACKAGE' ORDER BY 1;
EXIT;" | sqlplus -S sys/password@... as sysdba > all_packages.txt