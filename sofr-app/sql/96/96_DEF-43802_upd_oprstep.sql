--Обновление записи DOPROSTEP_DBT
BEGIN
   UPDATE DOPROSTEP_DBT
      SET T_DAYFLAG = CHR (0)
    WHERE T_BLOCKID IN (205300, 205200, 205100, 281000, 281100)
          AND T_DAYFLAG = CHR (88);
EXCEPTION
   WHEN OTHERS
   THEN
      NULL;
END;
/