CREATE OR REPLACE package it_generatcntrlpariedacc is
  
   DL_DVDEAL           CONSTANT INTEGER := 192;
   DL_DVNDEAL          CONSTANT INTEGER := 199;
   
   DL_DVNDEALPMGR      CONSTANT INTEGER := 4811; 
   DL_DVDEALT3         CONSTANT INTEGER := 4815;
   
   DL_IBCDOC           CONSTANT INTEGER := 102;
   DL_CREDITLN         CONSTANT INTEGER := 208;
   DL_MMPAWN           CONSTANT INTEGER := 126;
   
   DL_GENAGRFOREXDOC   CONSTANT INTEGER := 4610;
   DL_GENAGRIBCDOC     CONSTANT INTEGER := 4611;
   DL_VEKSELDRAWORDER  CONSTANT INTEGER := 110;
   DL_VEKSELACCOUNTED  CONSTANT INTEGER := 141;
   DL_VAREPAY          CONSTANT INTEGER := 142;

   FUNCTION IsDV(pDocKind IN NUMBER) RETURN NUMBER DETERMINISTIC;
   FUNCTION IsMmark(pDocKind IN NUMBER) RETURN NUMBER DETERMINISTIC;
   FUNCTION IsSecur(pDocKind IN NUMBER) RETURN NUMBER DETERMINISTIC;
   FUNCTION IsBankVeksel(pDocKind IN NUMBER) RETURN NUMBER DETERMINISTIC;
   FUNCTION IsAccVeksel(pDocKind IN NUMBER) RETURN NUMBER DETERMINISTIC;

   procedure ReportRun(pRepDate in date, pIsSecur IN CHAR, pIsDV IN CHAR, pIsMMark IN CHAR, pIsBankVeksel IN CHAR, pIsAccVeksel IN CHAR, oGuID out varchar2, oErrorCode out NUMBER, oErrorDesc OUT VARCHAR2);  
   
end it_generatcntrlpariedacc;
/
