/*
$Name:             add_agr.mac
$Module:           Кредитование
$Description:      Дополнительное соглашение
*/
import total;

private record add_agr ("add_agr.dbt", "loans.def");
private record cur_credit ("credit_c", "loans.def");

macro PrintLoansReport(buf, FromWeb)

      if (FromWeb == true)
          return "";
      end;

      SetBuff(add_agr, buf);
end;
