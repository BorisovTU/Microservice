/*
  Mакрос печати из скроллинга графика погашения комиссий.
  Вызов по F7 из режима просмотра графика погашения комисии.
  Вызываемая функция ПечатьГрафикИзмененияЛимита (ObjectTypeID, ObjectID, TypeGraph)
  Параметры: (соответствуют dgraphlim_dbt)
  ObjectTypeID: вид объекта
  ObjectID    : номер объекта
  TypeGraph   : тип графика
  OperID      : идентификатор операции
*/


MACRO ПечатьГрафикИзмененияЛимита (ObjectTypeID, ObjectID, TypeGraph, OperID)
  exit(0);
END;

