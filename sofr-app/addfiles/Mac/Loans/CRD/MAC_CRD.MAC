/*макрос инициализации при вводе вида кредита в панели ввода договора
 и проверки при вводе\корректировке кредитного договора*/

record OldCredit ("credit_c.dbt", "loans.def");  /* буфер договора до обновления */
record Credit    ("credit_c.dbt", "loans.def");  /* текущий буфер договора       */

macro Договор( Режим )
  /* 0 - инициализация договора при выборе вида кредита,
     1 - проверки при вводе договора,
     2 - проверки при корректировке договора */
  return 0;
end;