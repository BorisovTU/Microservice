/*
$Name:             P_CHSUM.mac
$Module:           Кредитование
$Description:      Изменение суммы договора/гарантии
*/

import CFormInter;

MACRO Init(buff)
   RECORD credit_c ("credit_c.dbt");

   setBuff(credit_c, buff);

   setFieldValue(1, credit_c.CreditSum);

   RETURN TRUE;
END;