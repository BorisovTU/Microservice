/*
$Name: DLCLAIMSTAGE_DBT.mac
$Module: Кредитование
$Description: Структура таблицы DLCLAIMSTAGE_DBT
*/
CLASS DLCLAIMSTAGE_DBT()
	// Идентификатор стадии
	var id: integer = 0;

	// Порядковый номер стадии
	var num: integer = 0;

	// Название стадии
	var name: string = "";

	// Признак использования
	var isnotuse: string = "";

	// Признак обновления системной стадии
	var isupdate: string = "";
END;