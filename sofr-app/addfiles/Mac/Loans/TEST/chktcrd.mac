/*------------------------------------------------------------------------------------------------
Filename    : chktcrd.mac
Description : Макрос инициализации, проверки. 
		    Вызывается при выборе/вводе/обновлении/удалении вида кредита.
Return	  : bool (true - успешное завершение, false - ошибка).
-------------------------------------------------------------------------------------------------*/

record СтарыйБуфер("type_crd.dbt", "loans.def"); /* Буфер вида кредита до обновления/вставки */
record Буфер  ("type_crd.dbt", "loans.def");     /* Текущий буфер вида кредита               */

/* Проверка перед вводом вида кредита */
macro __Init__  
  MsgBox("chktcrd.mac: __Init__");	
  return true;
end;

/* Проверка при вводе */  
macro __Check_Input__
  MsgBox("chktcrd.mac: __Check_Input__");
  return true;
end;

/* Проверка при обновлении */
macro __Check_Edit__ 
  MsgBox("chktcrd.mac: __Check_Edit__");
  return true;
end;

/* Проверка при удалении */
macro __Check_Delete__
  MsgBox("chktcrd.mac: __Check_Delete__");
  return true;
end;