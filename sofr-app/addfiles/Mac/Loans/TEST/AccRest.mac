IMPORT SbCrdInter, total;
record IgCrdAcc("IgCrdAcc.dbt", "loans.def"); //Связь КД,СПИ и типа счета других подсистем
record IgSetAcc("IgSetAcc.dbt", "loans.def"); //Счета для расчетов с субъектами
record Credit("credit_c.dbt", "loans.def"); //кридитный договор

//возвращает сумму счета
macro GetBankAccRest()
      return 100500.12;
end;


