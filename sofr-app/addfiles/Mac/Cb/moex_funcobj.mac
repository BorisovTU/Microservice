/*
 $Name: moex_funcobj.mac
 $Module: Ядро Banking
 $Description: Обработка ответа от модуля регистрации клиентов в MOEX
 */
import "dlcontrmsg_mmvb.mac";

// макрос обработки ответа от модуля регистрации клиентов в MOEX
macro RegClientResult(objectType, objectId, param, guid, taskId)
  return DLOnlineMesResponseProc(objectType, objectId, param, guid, taskId);
end;