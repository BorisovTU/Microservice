import CTinter;

record          ТО(sfinv);
record          СтароеТО(sfinv);

macro   Новое_ТО()
        return 0;
end;

macro   Проверить_ТО(Mode)
  return 0;
end;

private macro   Функция_Пользователя()
        return 0;
end;