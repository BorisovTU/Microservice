Import RsbDataSet, "sfcommon.mac", "sfbatch_lib.mac";

record SfSrvDocRec("sfsrvdoc.dbt");

macro MassExecuteStep()
  var stat:integer = 0;
 
  stat = SfPercomSetOprStatus( SfSrvDocRec ) ;
             
  return stat;
end;
