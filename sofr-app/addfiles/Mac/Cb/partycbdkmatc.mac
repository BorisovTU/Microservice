/*
 * Пример макроса квитовки синхронизации физических лиц в ЦБД клиентов объявленного 
 * в настройке CB/PARTY/CBDK/PERSON/MATCMACROPERSON
 */

import PTInter;

/* Функция квитовки синхронизации физических лиц 
 */
macro PartyMatc()
  return 0;
end;