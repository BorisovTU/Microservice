import BankInter;

private Record Symbol( symbcash );
private Record Save_Symbol( symbcash );

/*
 * Режимы: 
 * OBJ_INSERT - вставка
 * OBJ_UPDATE - редактирование
 * OBJ_DELETE - удаление 
 */
private macro Check_Symbol( Режим )
  return 0;
end;
