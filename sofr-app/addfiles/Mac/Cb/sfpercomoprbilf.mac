import SfInter, BankInter, RsbDataSet, "globals.mac", BilFacturaInter;
import "sfbatch_lib.mac";

record SfSrvDocRec("sfsrvdoc.dbt");


macro MassExecuteStep()
  var stat = 0;

  stat = SFCreateBilFacturaBatch(SF_FEE_TYPE_PERIOD);

  return stat;
end;
