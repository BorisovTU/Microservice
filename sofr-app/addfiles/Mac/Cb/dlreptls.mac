/*поискать шаблон по путям из настроек и вернуть путь, по которому он был найден
(только для двухзвенки)*/
import BankInter, dlreptl;
MACRO DLREPTLS_FillTemplsPaths()
  var err = 0;
  GetRegistryValue("BANK_INI\\ОБЩИЕ ПАРАМЕТРЫ\\ДИРЕКТОРИИ\\USERTEMPLSDIR", V_STRING, DLREPTL_UserTemplsDir, err);
  GetRegistryValue("BANK_INI\\ОБЩИЕ ПАРАМЕТРЫ\\ДИРЕКТОРИИ\\TEMPLSDIR", V_STRING, DLREPTL_TemplsDir, err);
  return err;
END;