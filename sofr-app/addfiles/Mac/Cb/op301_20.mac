/*
 *  Макрос шага "Контроль" операции "Исправительный оборот" по первичному документу "Мемордер"
 */
Import BankInter, PaymInter;

var PaymentObj : RsbPayment;

macro ExecuteStep( doc, primdoc )
  
  if( PM_NeedRejectControl() == true )
    msgbox("Отвержение шага невозможно");
    return 1;
  end;
  
  /* var Memorial : RsbMemorialOrder = RsbMemorialOrder( PaymentObj.DocumentID ); */
  return 0;

end;