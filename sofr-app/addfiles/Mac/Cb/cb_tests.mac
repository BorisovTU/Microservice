/**
 @file 		cb_test.mac
 @brief 	Авто-тесты
*/

/** 
  @brief 		Простой авто-тест
                        Функция не принимает параметров.
                        Функция не должна работать интерактивно:
                        нельзя запрашивать информации у пользователя, нельзя ничего выводить на экран.
                        Можно записывать логи в базу.
                        Ошибки должны быть обработаны.

  @return 		0, если тест прошел успешно, 1, если не успешно
*/
MACRO SimpleTest
  return 0;
ONERROR(err)
  return 1;
END;

