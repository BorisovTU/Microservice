/*
 *  Макрос шага "Контроль" операции "Исправительный оборот" по первичному документу "Мультивалютный документ"
 */

import BankInter, PaymInter;

var PaymentObj : RsbPayment;

macro ExecuteStep( doc, primdoc )

  /*var Multy : RsbMultyDoc = RsbMultyDoc( PaymentObj.DocumentID );*/
  return 0;

end;