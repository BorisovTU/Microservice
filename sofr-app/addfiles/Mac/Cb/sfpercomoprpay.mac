import SfInter, "sfcrpaybatch.mac";

record SfSrvDocRec("sfsrvdoc.dbt");

macro MassExecuteStep()
  var stat = 0;

  stat = SFCreateDocPayBatch(SF_FEE_TYPE_PERIOD, NULL, NULL, SfSrvDocRec);

  return stat;
end;
