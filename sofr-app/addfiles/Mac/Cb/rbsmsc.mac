/*
$Name:           rbsmsc.mac
$Module:         
$Description:    Печать отчета по массовым действиям для сводных мем. ордеров 
*/ 

IMPORT PaymInter, "gstname.mac";
IMPORT "d_inter.mac";

RECORD paym(pmpaym);

MACRO Печать_Отчета(Вызов,firstdoc,Статус,Сообщение, Заголовок, УспСообщение)

  setbuff(paym,firstdoc);

  ПечатьПолногоОтчетаДляДокумента (Вызов,Статус,Сообщение,УспСообщение,Заголовок,paym.PaymentID);

end;



