//-----------------------------------------------------------------------------
// Блок     : 29045 - "Картотека требований-поручений"
// Шаг      : 10    - "Постановка на картотеку требований"
// Описание : Макрос шага
//-----------------------------------------------------------------------------
import globals, PaymInter;

var PaymentObj:RsbPayment;

// ----------------------------------------------------------------------------
// Макроса шага
// ----------------------------------------------------------------------------
MACRO ExecuteStep( doc, paymDoc )

  var stat:integer = 0;
  
  if( PaymentObj.StartDepartment != {OperDprt} )
    msgbox( "Шаг может быть выполнен только из филиала,|в котором было создано требование" ); 
    return 1;
  end;

  PaymentObj.DemandIndexPlaceDate = {curdate};

  return 0;
END;
