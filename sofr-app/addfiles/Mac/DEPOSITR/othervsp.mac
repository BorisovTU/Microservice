import alg_vars;

if ( not GetTrue( True, "Вкладчик согласен на предоставление ему права обслуживания|в других подразделениях банка?" ) )

  ALG_CONTRACT.Flags = ALG_CONTRACT.Flags + "D";
  ALG_CONTRACT.Update;

end;

