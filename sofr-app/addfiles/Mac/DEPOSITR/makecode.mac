/*
  Пример макроса формирования пользовательского кода клиента.
  Внимание!
  ~~~~~~~~~
  1. Имя макропроцедуры должно обязательно совпадать с именем макроса.
  2. Макропроцедура обязательно должна возвращать:
     True  - код клиента сформирован.
     False - при формировании кода клиента возникла ошибка.
*/
import CommonInter;

macro makecode( dep:TDepClient ):bool
  if (GetInt (dep.rec.CodClient, "Введите код клиента!") )
    return True;
  else
    return False;
  end;
end;
