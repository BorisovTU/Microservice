
/* Завершение года. Ревизия пенсионных. Выполнение шага */
 
const
  OK_RES   =  0,  /* продолжать проводку */
  SKIP_RES =  1,  /* пропустить выполнение предопределенного модуля */
  END_RES  =  2,  /* завершить проводку */
  ERR_RES  = -1;  /* аваpийно завершить проводку */

var
  Stat;

  Stat = ExecMacroFile( "pensinsm.mac", "ревизияПенсионных", false, true, false );
  if ( Stat )
    ALG_RESULT = OK_RES;
  else
    ALG_RESULT = ERR_RES;
  end;
end;
