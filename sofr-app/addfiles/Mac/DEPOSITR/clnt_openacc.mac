/***************************************************************************

  R-Style SoftLab.

  RS-Retail.

  Пример макроса, вызываемого в панели "Выбор вкладчика" при открытии счета: 
  при нажатии F3 для списка клиентов.

****************************************************************************/

macro Init( pClntCode )  // Код клиента.
  // msgbox( "clnt_openacc.mac = ", pClntCode );
end;
