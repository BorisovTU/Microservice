/*

      R-Style SoftLab.

      RS-Bank.v.6 (Oracle)

      RS-Retail.

      Макрос проверки при открытии на нулевую сумму.

      Используется для текущих и спец.счетов.

*/

 import Alg_Vars, deprintr;

 ALG_RESULT = OK_RES;

 if ( ( Index( ALG_DEPOSITOR.UserTypeAccount, "Щ" ) > 0 )  AND
      ( ALG_SBDEPDOC.InSum != $0.0 ) )
      MsgBox( "Счет вида вкладов \"", ALG_DEPOSITOR.Type_Account,
              "\" открывается только на нулевую сумму." );
      ALG_RESULT = ERR_RES;
 end;
