/*

   R-Style SoftLab

   RS-Retail

   Макрос выпуска ф.31 и ф.187 при открытии "Блокированного целевого для ГЖС"

   Ввод суммы ГЖС

*/

import DeposInter;

record ALG_SBDEPDOC ( "sbdepdoc.1" );

ALG_SBDEPDOC.OutSum = $0;

UpdateAlgRecords( 4, TRUE, TRUE );

