/*------------- Печать доверенности/ завещания -------------------*/
/*          18 фев. 1997 Клепиков С.А. (aKa MustDiE!)             */

import DeprIntr, FirstLog,"print_trust.mac";

 ALG_RESULT = 0; 
 
 if ( ALG_DEPOSITOR.CodClient )
   Copy( d, ALG_DEPOSITOR );
   Copy( t, ALG_TRAST );

   Pr_TrastExe( ALG_DEPOSITOR.CodClient );
 end;

END;
