import GetMComm;

/*════════════════════════════╗
║   Точка входа в программу   ║
╚════════════════════════════*/

ИнициализироватьИсхДанные();
СоздатьФайлы();
ВыгрузитьСистемныеМодули();
ВыгрузитьМеню();

MsgBox(  string( "Выгружено :|",
         itemsCount:5, " записей системных модулей          |",
         menuCount :5, " записей для меню операциониста "+string(menuOper) ));

Exit( 1);
