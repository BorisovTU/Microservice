/**
  @file dofr_unload_man.mac
  @brief Выгрузка сделок ДОФР

  Выгрузка сделок ДОФР в CSV формат

  #tag
  - functional_block:ДОФР

  #changelog
*/

import "dofr_unload.mac";

var pnl = ManualUnloadPanel(); 
pnl.Run();
exit(1);