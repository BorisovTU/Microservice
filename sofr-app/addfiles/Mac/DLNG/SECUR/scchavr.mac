/* Операция "Расчеты по сделке"
   шаг "Переносить поставку по срокам ?" 
   для сделок без обр. части или по первой части*/
macro ExecuteCaseStep( Kind_Operation, Number_Step, FDoc, KindDoc )

//  return string(Kind_Operation) + "00:" + ОпределитьШагПереносаПоСрокам( FD, "70", "60", FD.pm_avoir, null, null, null, null, null, FD.pm_money );
  return string(Kind_Operation) + "00:" + "70";
end;
