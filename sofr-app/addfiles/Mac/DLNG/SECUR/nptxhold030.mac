/* Операция удержания НДФЛ
   Шаг "Корректировка удерживаемого налога" */
MACRO ExecuteStep( DlDoc, FDoc, DocKind, ID_Operation, ID_Step )

  VAR Oper = TRecHandler( "nptxop.dbt" );

  Oper.SetRecordAddr( FDoc );

  return 0;
END;

