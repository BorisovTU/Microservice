/*
  Получение номера документа (поле Xld в spground)
*/

macro CreateDraftXld( Xld: string )

  /* 
    Xld - уже сформированный уникальный номер (по ID чего либо). 
    Можно поправить и вернуть через return.
  */
  return Xld;

end