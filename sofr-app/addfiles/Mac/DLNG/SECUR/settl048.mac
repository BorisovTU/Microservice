/* Операция Расчеты на ОРЦБ         
   Шаг - Формирование сводных платежей ДЕПО по сделкам*/
import "sp_carsd.mac";

macro ExecuteStep( Doc, adr )
   record comm( dl_comm );
   var    FD, DepParm;

   SetBuff( comm, adr );

 
   return 0;
end;

