/*
  Формирование номера поручения депо 
  31.07.2000  Леонов И. Е.
*/                                                                          

macro GenDepoNum( Value, ObjKind, AddrBuf )

  record DepoDraft( spdraft );

  SetBuff( DepoDraft, AddrBuf );

  return string( Value );

end;