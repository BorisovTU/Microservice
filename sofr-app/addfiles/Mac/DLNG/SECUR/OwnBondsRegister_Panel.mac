/*
 $Name: OwnBondsRegister_Panel.mac
 $Module: Ценные бумаги
 $Description: Прослойка для запуска отчета из RS-Bank и из WEB-сервиса НУ "Расчет доходов и расходов по собственным облигациям"
*/

import "ownbondsregister.mac";

macro ReportRun()
  ReportRunEx(false);
end;

ReportRun();
Exit(1);
