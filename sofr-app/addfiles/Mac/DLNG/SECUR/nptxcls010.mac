/* Операция закрытия налоговых периодов для НДФЛ
   Шаг инициализации           */

import InsCarryDoc, "sp_categ.mac", "sp_car.mac", RsbDataSet;


macro ExecuteParamStep( kind_oper, DocKind, FDoc )

  return 0;
end;
