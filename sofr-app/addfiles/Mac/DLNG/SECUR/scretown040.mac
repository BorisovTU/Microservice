/*
$Name:         scretown040.mac 
$Module:       Ценные бумаги
$Description:  Операция погашения выпуска СЭБ. Учет комиссии
*/
import "spretownfun.mac";

macro ExecuteStep( Doc, FDoc, DocKind, ID_Operation, ID_Step )
  record deal(dl_tick);
  var FD, dat = {curdate};
  var WasInsGr = false;

  SetBuff( deal, FDoc );
  FD = SPFirstDoc ( deal, false );

  dat = FD.dl_leg.rec.MoveDate;

  return 0;
end;