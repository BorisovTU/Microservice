/* Операция списания затрат
   Шаг настройки операции           */
import SPInter;

macro executeParamStep( kind_oper, BOf_kind, FDoc )

  record  dl_comm(dl_comm);

  SetBuff( dl_comm, FDoc ); 

  MsgBox( "Запрещено выполнение операции" );/*в связи с удалением настройки "Учёт затрат в целом по торговому портфелю" операция теряет смысл*/
  return 1;
end;
