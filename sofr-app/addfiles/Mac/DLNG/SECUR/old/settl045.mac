/*
$Name:         settl045.mac
$Module:       Ценные бумаги
$Description:  не используется
*/
/* Операция Расчеты на ОРЦБ         
   Шаг - Закрывать счета расчетов с биржей ? */

macro ExecuteCaseStep( Kind_Operation, Number_Step, FDoc, KindDoc )

  record comm( dl_comm );

  if( GetTrue( true, "Пополнить торговый счет ?" ) )  
     return "40";
  end;  

  return "47"; /*Сводные проводки*/
end;