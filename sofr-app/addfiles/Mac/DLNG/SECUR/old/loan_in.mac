/*
$Name:         loan_in.mac
$Module:       Ценные бумаги
$Description:  не используется
*/
/* инициализация операций займа*/
IMPORT "sp_class.mac", SFInter;

MACRO InitOperation( KindOpr, KindDoc, FDoc )

   RECORD Deal(dl_tick);
   var    FD, ContrNumber = "";

   SetBuff( Deal, FDoc );   
   FD = SPFirstDoc( Deal ); 

   /*УсловияСделки - глобальная структура, описанная в макросе скроллинга (spDeal.mac)*/
   copy( FD.dl_leg, УсловияСделки );

   return 0;
END;