/*
$Name:             txcheck_all.mac
$Module:           БОЦБ 
$Description:      Обертка для вызова "Проверка данных для репликации"
*/
import ws_file;

macro RunWebReport(MySessID:string)
    var FullReportFileNames;
    
    ExecMacroFile("txcheck_all_exec.mac", "TxRunReportBeforeRepl", true, @FullReportFileNames, MySessID);
    
    return FullReportFileNames;
end;
