/* Операция получения паев
   Шаг ожидания параметров*/
macro ExecuteStep( doc, FDoc)

  msgbox("Запрещено выполнять этот шаг.|Ожидается выполнение расчетов \"Определение параметров \" по сделке.");    
  return 1;
end;
