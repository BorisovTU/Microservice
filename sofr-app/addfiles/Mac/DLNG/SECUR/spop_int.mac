/*AV 26.02.02*/
/* общий макрос инициализации операций ПЕРЕВОДА ценных бумаг */

IMPORT OprInter, PaymInter, CTInter, SPinter, "sp_categ.mac";

RECORD dl_comm_trans("dlcomsum.str"); 

MACRO InitOperation( KindOpr, KindDoc, FDoc )

    RECORD comm(dl_comm);

    SetBuff( comm, FDoc );

    return 0;
END;