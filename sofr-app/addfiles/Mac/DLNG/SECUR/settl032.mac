/* Операция Расчеты на ОРЦБ         
   Шаг - перевод средств на клиринговый счет*/
import "settl.mac";

macro ExecuteStep( Doc, adr )
  VAR comm = TRecHandler( "dl_comm.dbt" );

  comm.SetRecordAddr( adr );

  if( not ПополнитьКлиринговыйСчет( Doc, comm ) )
     return 1;
  end;

  return 0;
end;
