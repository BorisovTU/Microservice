/*
$Name:        mmclpenalt.mac
$Module:      МБ Кредиты
$Description: макрос "Генерация штрафов"
*/

IMPORT mmpenalt;


MACRO ExecutePn(DealID)
  VAR rs;
          
    rs = TRsbDataSet("select t_DealID from ddl_tick_dbt tick where" +
                    " tick.t_ParentID = " + String(DealID));
    /*
    while(rs.MoveNext())
        Execute(rs.DealID);
    end;*/

  return true;
END;

NameMacroFile = "mmclpenalt.mac";
