/*
$Name: scregvotescn.mac
$Module: Депозитарий
$Description: Массовое выполнение действий над записыми по голосованию в списке-реестре
*/
IMPORT FIInter, DPInter, dlquery;   
 
macro Печать_ОтчетСканирования (Вызов, doc, Статус, Сообщение, Действие)
   if (Вызов == 1) //Удаление
      exit(1);
   end;
end;