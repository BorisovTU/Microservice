import fxstep, fxcomiss;
record deal(dl_tick);

MACRO ExecuteCaseStep(OperationKind, StepNumber, Document, DocKind)
  SetBuff( deal, Document );
  return FX_ExecuteStepFunction(deal.DealID, DL_LEGID_BASE, "Get_Comiss");
END;