/*
$Name: dl_rep_Prolong_Hist_FromDeal.mac
$Module: Deals
$Description: Отчет об истории пролонгаций (вызов из скроллинга/панели)
*/

import "dl_rep_Prolong_History.mac";

MACRO ReportFromDeal(DealID)
    var rep = CReport(DealID);
    rep.GenerateReport();
    exit(1);
END;