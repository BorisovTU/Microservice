/*
$Name:        dvnop040.mac
$Module:      Производные инструменты
$Description: Внебиржевая операция. Шаг: Перенос по срокам
*/
import "dv_period.mac";

macro ExecuteStep( doc, FDoc, DocKind, ID_Operation, ID_Step )

  record ndeal(dvndeal);

  SetBuff( ndeal, FDoc );

  return ВыполнитьПереносПоСрокам( doc, DVFirstDocNDeal(DocKind, ndeal) );
end;
