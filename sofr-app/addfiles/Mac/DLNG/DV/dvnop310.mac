/*
$Name:        dvnop310.mac
$Module:      Производные инструменты
$Description: Внебиржевая операция. Шаг: Переоценка справедливой стоимости ПФИ
*/
import InsCarryDoc, "dvoverfv.mac";

macro ExecuteStep( doc, FDoc, DocKind, ID_Operation, ID_Step )
  return ВыполнитьПереоценкуСС( doc, FDoc, DocKind );
end;
