/*
$Name:        dvopcur020.mac
$Module:      Производные инструменты
$Description: Расчеты по производным инструментам на валютном рынке/рынке СПФИ. Шаг "Сводные проводки"
*/
import "dv_carsm.mac";

macro ExecuteStep( doc, FDoc, DocKind, ID_Operation, ID_Step )

  record oper(dvoper);

  SetBuff(oper, FDoc);

  var FD_Oper = DVFirstDocOper(DL_DVOPER, oper);

  return ВыполнитьСводныеПроводки(FD_Oper, doc, oper.Party, oper.Date);
end;
