/*
$Name:        dvnop040_2.mac
$Module:      Производные инструменты
$Description: Внебиржевая операция. Шаг: Перенос по срокам 2й части
*/
import "dv_period.mac";

macro ExecuteStep( doc, FDoc, DocKind, ID_Operation, ID_Step )

  record ndeal(dvndeal);

  SetBuff( ndeal, FDoc );

  return ВыполнитьПереносПоСрокам( doc, DVFirstDocNDeal(DocKind, ndeal, 2) );
end;
