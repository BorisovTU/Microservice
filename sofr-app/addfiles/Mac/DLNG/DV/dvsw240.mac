/*
$Name:        dvsw240.mac
$Module:      Производные инструменты
$Description: Процентный СВОП. Шаг: Перенос по срокам ПП
*/
import "dv_period.mac";

macro ExecuteStep( doc, FDoc, DocKind, ID_Operation, ID_Step )

  record ndeal(dvndeal);
  SetBuff( ndeal, FDoc );

  return ВыполнитьПереносПоСрокамПП( doc, DVFirstDocNDeal(DocKind, ndeal, 2) );
end;
