/*    Банкнотная сделка */
/*   Шаг выбора акцепта обязательства*/
import InsCarryDoc, "dvserv.mac", "dv_fun.mac", "dv_carop.mac";

macro ExecuteCaseStep( Kind_Operation, Number_Step, FDoc, KindDoc)

   record deal(dvndeal);
   SetBuff(deal, FDoc);
   var FD = DVFirstDocFXDeal(DL_DVFXDEAL, deal);

   if( FD.IsSale() )
         return "200"; /*шаг акцепт в кассе*/  
   end;
  
  return 0;
end;
