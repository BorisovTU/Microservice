/*
$Name:        dvcuritog020.mac
$Module:      ФИССИКО
$Description: Операция "Обработка итогов биржевых валютных торгов", Шаг Взаимозачет встречных обязательств по ПФИ
*/
import "dv_car.mac";

MACRO ExecuteStep( Doc, FirstDoc, DocKind, ID_Operation, ID_Step )

  record Comm(dl_comm);
  SetBuff(Comm, FirstDoc);
  if( DV_LinkNDeals(Comm.DocumentID) != 0 )
     return 1;
  end;
  return 0;
end;

