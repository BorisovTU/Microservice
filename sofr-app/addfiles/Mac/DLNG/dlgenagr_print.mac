/*
$Name:         dlgenagr_print.mac
$Module:       Ядро Securities
$Description:  Макрос печати Генерального соглашения
*/

import "sp_class.mac", "SpRepFn2.mac", SecInter;

private record dlGenAgr( DL_GENAGR );

macro Print_GenAgr( genAgr )
   
	SetBuff( dlGenAgr, genAgr );
  
	MsgBox( "Печать не реализована. Выполняется макрофункция \"dlgenagr_print.mac\\ Функция печати Print_GenAgr\"." );
    return 0;
end;