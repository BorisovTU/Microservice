/* Операция сопровождения ИДДУ
   Шаг "Переоценка активов"*/
IMPORT InsCarryDoc, TSInter;

MACRO ExecuteStep( DlDoc, FDoc, DocKind, ID_Operation, ID_Step )

  RECORD Order( tsorder );
  SetBuff( Order, FDoc );   

  return 0;
END;

