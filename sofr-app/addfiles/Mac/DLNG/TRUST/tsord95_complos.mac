/* Операция сопровождения ДП ОФБУ */
/* Компенсации убытка             */

import "tscomplos.mac";

MACRO ExecuteStep( DlDoc, FDoc, DocKind, ID_Operation, ID_Step )

  return ВыполнитьКомпенсациюУбытка( DlDoc, FDoc, ID_Operation, ID_Step );
END;
