/* Расчеты на ОРЦБ в ДУ
   Завершение операции*/
import InsCarryDoc;
 
macro ExecuteStep( doc, FDoc, DocKind, _OperationID, ID_Step )
  VAR Oper = TRecHandler( "dl_comm" );

  Oper.SetRecordAddr( FDoc );
  
  return 0;
end;
