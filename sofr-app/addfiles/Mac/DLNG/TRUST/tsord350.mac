/* Корректировка СЧА */

IMPORT "tscorrcost.mac";

MACRO ExecuteStep( DlDoc, FDoc, DocKind, ID_Operation, ID_Step )

  return ВыполнитьКорректировкуСЧА( DlDoc, FDoc, DocKind, ID_Operation, ID_Step, NULL );
END;
