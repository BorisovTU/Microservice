/* Операция сопровождения общих условий ПОФБУ 
   СНП - ожидание начисления комиссий */

MACRO ExecuteStep( DlDoc, FDoc, DocKind, ID_Operation, ID_Step )

  MsgBox( "Выполнение данного шага запрещено." );

  return 1;
END;
