/* Операция сопровождения ИДДУ
   Шаг "Оформление пролонгации", блок - "Пролонгация с выводом капитала"*/
IMPORT InsCarryDoc, "tsprolong.mac";

MACRO ExecuteStep( DlDoc, FDoc, DocKind, ID_Operation, ID_Step )

  VAR Order = TRecHandler( "tsorder.dbt" );

  Order.SetRecordAddr( FDoc );
  return ОформитьПролонгацию( Order, true );
END;

