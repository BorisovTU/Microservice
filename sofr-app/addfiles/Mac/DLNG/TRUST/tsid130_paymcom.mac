/* Операция сопровождения ИДДУ, ОУ ОФБУ, ДП ОФБУ */
/* Оплата комиссий             */

import "tspaymcom.mac";

MACRO ExecuteStep( DlDoc, FDoc, DocKind, ID_Operation, ID_Step )

  return ВыполнитьОплатуКомиссий( DlDoc, FDoc, ID_Operation, ID_Step );
END;
