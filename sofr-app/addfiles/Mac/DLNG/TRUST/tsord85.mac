/* Операция сопровождения ДП ОФБУ 
   Расчеты при пролонгации */
IMPORT "TSCalcPrCl.mac";

MACRO ExecuteStep( DlDoc, FDoc, DocKind, ID_Operation, ID_Step )

  return ОбработатьИтогиПЗ( DlDoc, FDoc, ID_Operation, ID_Step, 0 );
END;
