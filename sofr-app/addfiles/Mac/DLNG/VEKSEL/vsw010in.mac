/*
$Name:         vsw010in.mac
$Module:       Векселя банка
$Description:  Списание ранее выкупленных/не предъявленных векселей. Инициализация

            vs-вексель
             w-операция списание ранее выкупленных векселей (write off)
           010-номер (соответствующий шагу)
            in-инициализация (initialization)
*/

// Запуск шага
//
macro ExecuteStep(Buffer, fmo_buf)
    var stat = 0;

    // ...

    return stat;
end;

