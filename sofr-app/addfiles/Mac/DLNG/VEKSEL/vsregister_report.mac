/*
$Name:        vsregister_report.mac
$Module:      Векселя банка
$Description: Прослойка для запуска отчета "Регистр по собственным векселям"
*/
IMPORT "vsregister.mac";

macro ReportRun()
  ReportRunEx(false);
end;

ReportRun();
Exit(1);