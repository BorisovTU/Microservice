Import DealsInter, Календарь;

PRIVATE const NumOfCalendarYears = 1;

MACRO НайтиЦеновыеУсловия (leg, BCID)
    leg.rec.LegKind = LEG_KIND_VSBANNER;
    leg.rec.DealID = BCID;
    leg.rec.LegID = 0;
    return leg.GetEQ;
END;