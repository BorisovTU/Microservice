/*
$Name:        dldlreq.mac
$Module:      Ядро Securities
$Description: Макрос скроллинга заявок-поручений
*/

PRIVATE VAR Документ       = TRecHandler("dl_req");
PRIVATE VAR СтарыйДокумент = TRecHandler("dl_req");

/* Макрофункция вызывается по Ctrl-Z из скроллинга/панели */
PRIVATE MACRO Функция_Пользователя()
  return 0;
END;
