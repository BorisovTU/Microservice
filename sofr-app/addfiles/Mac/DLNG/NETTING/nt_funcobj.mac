/*
$Name:        nt_funcobj.mac
$Module:      Неттинг
$Description: закрытие сделок, исполненных в неттинге
*/
IMPORT FIInter, InsCarryDoc, "dlntlib.mac";
import DVInter;

MACRO RunDeals( param0, param1, param2 )
  RECORD ntg_buff ("dl_nett");
  var stat = 0;
   stat =  DV_ExecDeal(param1, false);
   if(stat == 9)      //конец файла
      stat = 0;
   end;

  return stat;
END;
