import "clsdate.mac";

private var {curdate};

if ( CloseAndOpenDay({curdate}+1) )
   println(string("Процедура завершения дня и открытия нового дня с датой ",({curdate}+1), " выполнена успешно."));
else
   println(string("Процедура завершения дня и открытия нового дня с датой ",({curdate}+1), " завершилась с ошибками."));
end;