/*Пример макроса, подключаемого в шагах проверки */
record dch(db_check);
var s;
macro RunCheck( Batchmode, rec ) 
  setbuff( dch, rec );
  if(Batchmode)
   s="пакетном"; 
  else
   s="одиночном";
  end;
  [Выполняется шаг № #### c параметром # в ############# режиме]
  (dch.ID, dch.Parameter, s);
end; /*macro*/