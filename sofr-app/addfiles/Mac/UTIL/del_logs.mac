
/* “¤ «¥­¨¥ «®£ä ©«®¢ */

import BankInter;

var
  ComSpec = GetEnv( "COMSPEC" ),
  LogDir;

  if ( GetRegistryValue( "RS-RETAIL\\‘…‚ˆ‘›…\\€•ˆ‚€–ˆŸ\\“’œ_Š_‹ƒ”€‰‹€Œ",  V_STRING, LogDir, null ) != V_STRING )
    Exit( 1 );
  end;
  if ( SubStr( LogDir, StrLen( LogDir ) ) != "\\" )
    LogDir = LogDir + "\\";
  end;                
  Run( ComSpec, " /c del " + LogDir + "*.log >nul" );
end;