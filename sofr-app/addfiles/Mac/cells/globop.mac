/* Глобальные переменные НЕ ВКЛЮЧАТЬ В МАКРОСЫ ОТЧЁТЫ */

var operation     = CreateObjectOfOperation();   /* Объект, необходимый для операций */
var bunch         = CreateOpFootprintObject();   /* Объект, необходимый для печати */
var rollbackdata  = CreateRollbackDataObject();  /* Обьект, необходимый для отката */
