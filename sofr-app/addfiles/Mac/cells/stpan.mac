/* Стандартный макрос, навешивающийся на панель ввода/редактирования договора
  и возвращающий названия панелей. Если у типа договора указан макрос настройки,
  то в нем должна быть определена функция GetFieldsName */
import RslCellObjects, CommonInter;

macro init(obj)

  //obj.rec.Number = String(obj.rec.ContractID);

  return true;
end;

macro GetFieldsName()

var strName1 = "Арендатор 1:",
    strName2 = "Арендатор 2:";
  
  return strName1 + strName2;
end;
