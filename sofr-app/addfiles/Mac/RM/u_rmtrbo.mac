/*
   Отчет о массовой откате операций по созданным по исполнению платежей
*/

import u_rmtls;

macro Печать_ОтчетСканирования (Вызов,firstdoc,Статус,Сообщение)
   PaymReportMassPmLink (
                 Вызов,
                 firstdoc,
                 Статус,
                 Сообщение,
                 "Отчет о массовом откате операции по платежу",
                 "Откат операции выполнен успешно",
                 ПечататьСообщениеОбУспешномВыполнении
              );
end;
