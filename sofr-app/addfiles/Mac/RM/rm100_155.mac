//-----------------------------------------------------------------------------
// Блок      : 29032 - "Перенаправление невыясненного"
// Шаг       : 155   - "Контроль перенаправленного платежа"
// Назначение: Макрос шага
// Описание  : Макрос шага
//-----------------------------------------------------------------------------
import RMInter, "cbsttls.mac", PaymInter;

var PaymentObj:RsbPayment;

//-----------------------------------------------------------------------------
// Выполнение шага
//-----------------------------------------------------------------------------
macro ExecuteStep( doc, first, KindDoc, ID_Operation, ID_Step  )

  var stat:integer = 0;

  if( not КонтрольПлатежа( PaymentObj, ID_Operation, ID_Step ) )
    MsgBox("Пользователь " + {Oper} + " не может контролировать платеж");
    return 1;
  end; 

  // Тут можно что-нибудь проверить

  return stat;
end;