import "rmconst.mac", PaymInter;

macro ExecuteCaseStep( Kind_Operation, Number_Step, first, KindDoc )

  RECORD payment( pmpaym ) ;
  SetBuff( payment, first );

  if ( payment.PaymStatus == PM_FINISHED )
     return "";
  else
     return String(НомерШагаКлассификацияПлатежаВКазначействе);
  end;

end;