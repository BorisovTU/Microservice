import AutoProcess;


// Закомментировать или раскомментировать 2 строки между началом и концом заглушки
// Начало заглушки
//msgbox("Обработка файлов срочного рынка без прикладных алминистраторов СОФРа ограничена на время переходного периода смены форматов файлов.");
//exit(1);
// Конец заглушки


AutoProcess("-dialogflag:1 -processtype:2");
exit(1);

