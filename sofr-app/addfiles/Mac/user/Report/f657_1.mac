import globals;

   if ({oper}==0)
      if (gettrue(true,"Формировать отчет на расчитнных данных ПКЛ (да) или запустить отдельный отчет (нет) ?"))
       execmacrofile("f657_1cb_proc.mac");      
     else
       execmacrofile("f657_1cb.mac");
     end; 
   else
     execmacrofile("f657_1cb_proc.mac");
   end;
   
