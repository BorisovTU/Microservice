  import rsbdataset, "scroll_getfltr.mac";
  var arr = TArray();
  arr(0) = "Номер";
  arr(1) = "Наименование";
  var cmd = RsdCommand (" select t_number,t_name from dsfcontr_dbt where t_id in (" 
                          + USR_GetList(" select t_partyid, t_shortname from dparty_dbt  where rownum < 1002 --t_partyid in (select t_partyid from dsfcontr_dbt)", arr) 
                          + ")");
  var rs = RSDRecordset(cmd,RSDVAL_CLIENT, RSDVAL_STATIC );
  RunScroll(rs);
