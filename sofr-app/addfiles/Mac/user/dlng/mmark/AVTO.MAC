IMPORT mmarkinter, OprInter; // об€зательно проимпортировать OprInter дл€ корректной работы

 VAR II;
//  II=MM_DealExecuteStep(1488, ""); //выполнение операции из отложенных или поиск и выполнение первого шага в цепочке со статусом "√отов к обработке"
  II = MM_DealExecuteStep(31, "Р"); //поиск и выполнение шага c заданным символом со статусом "√отов к обработке" 

 MSGBOX(II);
END;