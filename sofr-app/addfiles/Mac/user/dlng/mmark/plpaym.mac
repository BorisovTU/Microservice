import BankInter,  PaymInter,  CurrInter ;

macro PaymDlg2(Fiid, PaymentID);
  var stat, BankPayment, Payment;
            if (PaymentID >  0 )
  if(FIID)
    BankPayment = RsbBbCpOrder(PaymentID);
  else
    BankPayment = RsbBankPayment(PaymentID);
  end;
  Payment = BankPayment.Payment();
//  stat = PM_ProcessPanel( Payment );
  stat = PM_ProcessPanel(Payment, null, null, null, null, null, null, null, null, null, null, null, null, 4);
  return true;
            end;
end;

