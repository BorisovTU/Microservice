import dl_plib;
record mo( "uo_mem_o", "USEROP.DEF" );

MACRO Ex (Buffer, fd)

private var d00 ,d01,{curdate};
   SetBuff( mo,fd );
   d00=mo.Date_Carry;
   d01=„€’(mo.ground);
   execmacrofile("ss01pl.mac","spis_pl", d00,d01 ) ;

   return 0;
END;


