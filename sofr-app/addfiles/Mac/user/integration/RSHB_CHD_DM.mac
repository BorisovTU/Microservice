import globals, oralib, likepy;

if (not gettrue(false,"Начать выгрузкку в ЦХД по Драгоценным металам?"))
  msgbox ("Был отказ от выгрузки");  
  exit(1);       
end;


begaction (20, "Выгрузка данных");
  execSQL ("begin qb_dwh_export.export_Precious_Metals_Status_Add(1, trunc(sysdate));end;");
endaction;
msgbox ("Выгрузка окончена");
exit(1);     






