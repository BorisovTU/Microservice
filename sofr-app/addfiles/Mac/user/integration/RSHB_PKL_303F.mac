import globals, oralib, likepy;

macro FZOZ(dateIn) //303 форма
var Params:TArray;
Params = makeArray(SQLParam("", dateIn),
                   SQLParam("", dateIn) );
execSQL ("begin RSHB_F303_2CHD.RSHB_F303_2CHD_GEN (:date1, :Date2); end;",Params);
end;
var dateout = {curdate};
var stat = GetDate(dateout, "Введите дату отчета");
if (stat);
FZOZ(dateout);
else 
exit(1);
end;

