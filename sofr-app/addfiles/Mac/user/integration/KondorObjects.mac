class ExecDealAnswer()
  var dealid;
  var stat;
  var msg;
end;

macro MakeExecDealAnswer(dealid,stat,msg)
  var retObj = ExecDealAnswer();
  retObj.dealid = dealid;
  retObj.stat = stat;
  retObj.msg = msg;
  return retObj;
end;