import "UpdateStatusDeal.mac";  // Интеграция: сервис выгрузки статуса сделки


private file Сделка("dl_tick");

  ClearRecord( Сделка );
  Сделка.DealID = 103;
  if( GetEQ( Сделка ) )
     //интеграция: выгрузка статуса сделки при помещении в отложенные
     UpdateStatusDeal(CreateObjectForExport( Сделка, EXPORT_DEAL_ROLLED_BACK ), true);
  else
    var n = 0;
  end; 


