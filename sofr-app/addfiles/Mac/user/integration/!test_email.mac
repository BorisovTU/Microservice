//import "global_utils_intgr.mac";

import "u_old_email_utils.mac"; /* Функционал рассылки email старой версии */

private const CV_EMAIL_HEAD = "СОФР - тестовое сообщение";
private const CV_EMAIL_RECEIVER = "tolmasovia@intech.rshb.ru";

var v_email_processor = c_email_proc_env();

//v_email_processor.m_set_msg_head(CV_EMAIL_HEAD);
//v_email_processor.m_add_email_to_list(CV_EMAIL_RECEIVER);
//v_email_processor.m_add_row_to_msg_text("Это сообщение отправлено в целях тестирования.\nПожалуйста, не отвечайте на него.");

//SetRSTrace(true);

/* Сохраняем текст письма для отправки плановой процедурой */
//v_email_processor.m_save_to_submit();
/* Отправляем все не отправленные письма */
v_email_processor.m_submit_email();

//SetRSTrace(false);

msgbox("Done!");