import oralib;

private macro RunExp()
  var query, cmd;
  query = "declare res varchar2(1000); begin res:=rshb_reconciliation.exp(); end;";
  cmd = RSDCommand(query);
  cmd.execute();
end;

RunExp();
