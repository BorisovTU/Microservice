import pm_setst, cbsttls, PTInter, CashInter, "pm_answerret.mac", "pmfm.mac";

var PaymentObj:RsbPayment;


MACRO ExecuteStep( doc, paymDoc )

  PaymentObj.PaymStatus = 3100/*Обрабатывается*/;  

  return 0;
END;

