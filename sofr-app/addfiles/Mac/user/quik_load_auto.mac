/*
$Name:         quik_load_auto.mac
$Module:       Лимиты
$Description:  Загрузка лимитов QUIK из РСХБ Брокер
*/

import "lim_utils.mac";

macro RunProcess()
  var quikLoader = QuikDealsLoader();
  quikLoader.AutoStart();
  quikLoader = null;
OnError(err)
end;

