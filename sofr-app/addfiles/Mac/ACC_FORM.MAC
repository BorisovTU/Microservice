//
// Макрос формата строки номера счета.
//
// На 14.12.06 обрезает длину номера счета до 20 символов. - SCR #77525.
//

macro Acc_Form( Acc )
  if ( ValType( Acc ) == V_STRING )
    return SubStr( Trim(Acc), 1, 20 );
  else
    return Acc;
  end;
end;