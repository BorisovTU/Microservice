/*
$Name:           UCS_EXTR.mac
$Module:         UCS
$Description:    Расширенная выписка
*/
import  "UCS_COMMON.mac";
macro Transmit(param)
    var obj = UFXMsgExRep;
    Param.Result = 0;
    obj.Transmit(param);
    return 0;
end;
