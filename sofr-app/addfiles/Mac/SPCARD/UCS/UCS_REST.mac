/*
$Name:           UCS_REST.mac
$Module:         UCS
$Description:    €‹€‘
*/// €‹‹€‘
import  "UCS_COMMON.mac";
macro Transmit(param)
    var obj = UFXMsgBalance(false);
    obj.Transmit(param) ;
    return 0;  
end;

