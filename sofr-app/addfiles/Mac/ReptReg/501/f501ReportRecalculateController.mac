/*
$Name:          f501ReportRecalculateController.mac
$Module:        Отчеты ЦБ
$Description:   Форма 501. Пересчет после свода.
*/

class TF501ReportRecalculateController()
    macro calculate()
        objectFactoryInstance.createNormalizationController().execute();
    end;

    private macro constructorTF501ReportRecalculateController()
    end;

    constructorTF501ReportRecalculateController();
end;
