/*
$Name:          OfrReportRecalculateController.mac
$Module:        Отчеты ЦБ
$Description:   ОФР. Пересчет после свода.
*/

class (RcbReportCalculateController) TOfrReportRecalculateController()
    private macro pushControllers()
        m_partCalculateControllers.push_back(objectFactoryInstance.createPart1RecalculateController(this));
    end;

    private macro printAdditionalProtocolData()
    end;

    macro initialize() : Bool
        pushControllers();

        return m_partCalculateControllers.size > 0;
    end;

    private macro constructorTOfrReportRecalculateController()
        initRcbReportCalculateController();
    end;

    constructorTOfrReportRecalculateController();
end;

class (TOfrReportRecalculateController) TOfrReportRecalculateTotalsController()
    private macro pushControllers()
        m_partCalculateControllers.push_back(objectFactoryInstance.createPart1RecalculateTotalsController(this));
    end;

    private macro constructorTOfrReportRecalculateTotalsController()
        initTOfrReportRecalculateController();
    end;

    constructorTOfrReportRecalculateTotalsController();
end;