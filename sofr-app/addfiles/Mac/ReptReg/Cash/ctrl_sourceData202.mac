/**
 * Контроль первичных данных кассовых оборотов.
 * 
 */
import ReptcbInter;

import CFSourceDataControlOperation; 
import CFObjectFactory;
import testLib;

var profiler = TSQLProfiler(getTxtFileName("!F202ControlSourceDataProfile"));
profiler.clear();
profiler.on();
                          
var controlOperation = objectFactoryInstance().createSourceDataControlOperation();

controlOperation.execute();

установитьФлагВозврата(OK_MACRO_FLAG);
exit(1);