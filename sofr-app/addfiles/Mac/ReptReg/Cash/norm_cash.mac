import ReptcbInter;
import RcbCoreInter;

import CFObjectFactory;
import CFNormalizationOperation;

if ((rcbApplication.currentReport.normalizationAlgorithm != RCB_NA_NONE) and
    (rcbApplication.currentReport.dimension != RCB_DIM_NATIONAL) and
    (rcbApplication.currentReport.dimension != RCB_DIM_ROUBLE))

    objectFactoryInstance.createNormalizationOperation().execute();
end;

установитьФлагВозврата(OK_MACRO_FLAG);
exit(1);