/**
 * Контроль рассчитанных данных кассовых оборотов.
 */
import ReptcbInter;

import CFCalculatedDataControlOperation; 
import CFObjectFactory;
                          
objectFactoryInstance().createCalculatedDataControlOperation().execute();

установитьФлагВозврата(OK_MACRO_FLAG);
exit(1);