/**
 * Контроль подразделений на наличие кодов ОКАТО.
 * 
 */
import ReptcbInter;
import RcbDepartmentOkatoControlOperation; 
                          
var controlOperation = genObject("TDepartmentOkatoControlOperation");

controlOperation.execute();

установитьФлагВозврата(OK_MACRO_FLAG);
exit(1);