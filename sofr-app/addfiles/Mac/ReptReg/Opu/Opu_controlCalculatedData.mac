/*
$Name:        Opu_controlCalculatedData.mac
$Module:      Регламентируемая отчетность
$Description: Запуск контроля рассчитанных данных
*/

import Opu_import;

objectFactoryInstance().createControlCalculatedData().execute();
var protocol = TSummaryProtocolView();
protocol.add(TProtocolView("ПРОТОКОЛ КОНТРОЛЯ РАССЧИТАННЫХ ДАННЫХ"));
protocol.show();

установитьФлагВозврата(OK_MACRO_FLAG);

exit(1);