/*
$Name:             wlmesedl.mac
$Module:           МБР
$Description:      Макрос скроллинга редактирования сообщения
*/

RECORD wledit    (wledit);
RECORD wleditold (wledit);

macro Проверить_Поле(Режим)
  var stat = 0;
  return 0;
end;

macro ProcessPanel(mode, key, field, panel)
  return key;
end;

private macro Функция_Пользователя( Режим )
  var stat = 0;
  return stat;
end;