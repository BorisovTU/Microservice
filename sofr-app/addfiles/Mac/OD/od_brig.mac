
/*
**  Опердневник ф.24 по текущей смене
**
*/

import od_main, sign_rep, chkboss;

ByeBoss(0);

var
  od = TBrigadeDiary( {oper} );

if ( not od.odShouldBeIssued )
  exitOnError( "По данной смене опердневник не выпускается" );
end;

od.proceed;

ElectronDigitalSignReport( );

exitMacro;