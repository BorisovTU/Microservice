/*──────────────────────────────────────────────────────────────────────────┐
  RS-Bank 5.1                                          R-Style Software Lab

  File Name   : chscand.mac
  Created     : 16.10.2000
  Programmer  : 
  Description : Печать отчета об удалении ценных бланков

└───────────────────────────────────────────────────────────────────────────*/


const   Ошибка   = 0,
    Первый   = 1,
    Последний= 2;

MACRO Печать_ОтчетСканирования (Вызов, firstdoc, Статус, Сообщение)
   array StringArray;
   var i = 1;

	RECORD m_checkiss (checkiss);

	setbuff (m_checkiss, firstdoc);


    if (Вызов==Первый)
[             Отчет об удалении ценных бланков.];
[+---------+--------+---------------------------------------------------------+];
[|    №    |   №    |                 Сообщение                               |];
[|документа| ошибки |                                                         |];
[+---------+--------+---------------------------------------------------------+];
    elif (Вызов==Ошибка)
        if (Статус == 0)
            Сообщение = "Удалено успешно"
         end;

         StrSplit(StrSubst(Сообщение, "|", " " ), StringArray, 56);
[|#########|########|#########################################################|](m_checkiss.Number, Статус, StringArray(0));
         while ( StrLen(StringArray(i) ) > 0)
[|         |        |#########################################################|](StringArray(i));
           i = i + 1;
         end;

        elif (Вызов==Последний)         
[+---------+--------+---------------------------------------------------------+];
    end;
END;