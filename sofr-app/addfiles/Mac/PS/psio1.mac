RECORD  cash_paym( pmpaym );
VAR     symbols:TArray = TArray(); //разбивка кассовых символов (только чтение)

macro ExecuteStep( doc, primdoc )
  //record          order(pscshdoc);
  //record          d (arhdoc);
  //SetBuff( order, primdoc );
  //SetBuff( d, doc );
  return 0;
end;