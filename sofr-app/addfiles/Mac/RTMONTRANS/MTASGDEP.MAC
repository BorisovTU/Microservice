import alg_vars, mtassign;

  var stat = MT_OperationExecution( 0 /*psId*/, MTOP_send_dep, 0 /*subOpNum*/, ALG_DEPOSITOR.Referenc );

  if ( stat != 0 )
    return ERR_RES;
  else
    return END_RES;
  end;
end;